

module types_m
  use const_m
  implicit none
  private

  !<< Time information >>!
  type, public :: time_t
     integer(DI) :: nloop      ! may run for lots of loops, in future.
     real(DR) :: dt
     real(DR) :: time
     real(DR) :: time_ppdata_2d
     real(DR) :: time_ppdata_3d
  end type

  type, public :: vfield_t
    type(sfield_t) :: x, y, z
  end type

  type, public :: pos_index_t
    integer(SI) :: i, j, k
  end type

end module
